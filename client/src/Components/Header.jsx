import React from "react";

export default function Header({ navbarSpot }) {
  return (
    <header
      style={{
      }}
    >
        {//Image will be added somewhere inside or near the header
}
      <h1 style={{}}>Food Finder</h1>
      {navbarSpot}
    </header>
  );
}